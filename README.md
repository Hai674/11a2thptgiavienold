# Lớp 11A10

Website lưu giữ thành viên, kỷ niệm, ảnh/video và lưu bút của lớp 11A10.

## Bắt đầu

1. Sao chép `.env.local.example` thành `.env.local` và điền thông tin Supabase khi cần.
2. Cài dependencies: `npm install`.
3. Chạy môi trường phát triển: `npm run dev`.
